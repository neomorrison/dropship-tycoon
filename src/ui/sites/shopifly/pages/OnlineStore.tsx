import type { ShopiflyPageProps } from '../route'

// PLACEHOLDER — replaced by the agent that owns this page.
export default function OnlineStore({ params }: ShopiflyPageProps) {
  return <div style={{ padding: 24 }}>OnlineStore {params.join('/')}</div>
}
