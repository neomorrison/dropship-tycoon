import type { ShopiflyPageProps } from '../route'

// PLACEHOLDER — replaced by the agent that owns this page.
export default function ProductEditor({ params }: ShopiflyPageProps) {
  return <div style={{ padding: 24 }}>ProductEditor {params.join('/')}</div>
}
