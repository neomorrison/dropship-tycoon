import type { ShopiflyPageProps } from '../route'

// PLACEHOLDER — replaced by the agent that owns this page.
export default function Reports({ params }: ShopiflyPageProps) {
  return <div style={{ padding: 24 }}>Reports {params.join('/')}</div>
}
