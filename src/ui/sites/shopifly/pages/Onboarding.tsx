import type { ShopiflyPageProps } from '../route'

// PLACEHOLDER — replaced by the agent that owns this page.
export default function Onboarding({ params }: ShopiflyPageProps) {
  return <div style={{ padding: 24 }}>Onboarding {params.join('/')}</div>
}
