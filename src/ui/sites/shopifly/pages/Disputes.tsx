import type { ShopiflyPageProps } from '../route'

// PLACEHOLDER — replaced by the agent that owns this page.
export default function Disputes({ params }: ShopiflyPageProps) {
  return <div style={{ padding: 24 }}>Disputes {params.join('/')}</div>
}
