import type { ShopiflyPageProps } from '../route'

// PLACEHOLDER — replaced by the agent that owns this page.
export default function Finances({ params }: ShopiflyPageProps) {
  return <div style={{ padding: 24 }}>Finances {params.join('/')}</div>
}
